# CentMinMod_Backup
Backup Sites on CMM

Folder where different paths are stored: /usr/local/src/centminmod_backup
Imp files for path: backup_path.conf - Where local backup path are stored , domain.com-ftp_path.conf - Where FTP details are saved, domain.com-backup.conf - Where mysql details are saved.

